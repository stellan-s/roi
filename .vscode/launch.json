{
  "version": "0.2.0",
  "configurations": [
    {
      "name": "Run Roi PoC",
      "type": "debugpy",
      "request": "launch",
      "program": "${workspaceFolder}/quant/main.py",
      "console": "integratedTerminal",
      "envFile": "${workspaceFolder}/.env"
    }
  ]
}
