run:
  as_of: today         # eller YYYY-MM-DD
  outdir: "reports"

data:
  cache_dir: "data"
  lookback_days: 500   # Back to original value to prevent hanging

signals:
  sma_long: 50         # Reduced from 200 to 50 for more responsive trend signal
  momentum_window: 126 # Reduced from 252 to 126 (6 months vs 12 months)
  news_feed_urls:
    - "https://news.cision.com/se/rss/all"

# Bayesian Engine Configuration
bayesian:
  time_horizon_days: 21          # Investeringshorisont för E[r] och Pr(↑)
  decision_thresholds:
    buy_probability: 0.55        # Köp när Pr(↑) ≥ 55% (more realistic)
    sell_probability: 0.45       # Sälj när Pr(↑) ≤ 45% (more realistic)
    min_expected_return: 0.0002  # Minimum 0.02% daglig E[r] för köp (more realistic)
    max_uncertainty: 0.50        # Max osäkerhet för action (accept more uncertainty)

  # Prior beliefs för signaler (kan justeras baserat på backtest)
  priors:
    trend_effectiveness: 0.62    # SMA trend-following edge (högre)
    momentum_effectiveness: 0.68 # Momentum är starkast dokumenterat (högre)
    sentiment_effectiveness: 0.58 # Sentiment är noisier (högre)

  # Parameter Learning (ersätter hardkodade värden med data-drivna estimat)
  parameter_learning:
    enabled: true                 # Aktivera adaptiv parameter-estimering
    calibration_lookback: 1000    # Dagar av data för parameter-kalibrering
    confidence_level: 0.95        # Konfidensnivå för parameter-estimat
    min_observations: 100         # Minimum datapunkter för tillförlitliga estimat

    # Signal-normalisering lärande
    sentiment_normalization: "percentile_based"  # Använd data-distribution vs hardkodad /2.0
    momentum_scaling: "distribution_based"       # Skala baserat på faktisk momentum-range

    # Regim-justering lärande
    regime_effectiveness_learning: true          # Lär regim-multiplikatorer från data
    bootstrap_iterations: 500                    # Bootstrap-sampel för konfidensintervall

# Regime Detection Configuration
regime_detection:
  enabled: true
  lookback_days: 60             # Regime detection window
  volatility_window: 20         # Increased from 10 to 20 for stability
  trend_window: 50              # Increased from 20 to 50 for better trends

  # Regime transition probabilities (sticky regimes)
  transition_persistence: 0.80  # Sannolikhet att stanna i samma regim

  # Regime classification thresholds (less aggressive)
  thresholds:
    volatility_low: 0.12        # 12% årlig vol = låg (lowered from 0.15)
    volatility_high: 0.30       # 30% årlig vol = hög (raised from 0.25)
    return_bull: 0.003          # 0.3% daglig för bull (raised from 0.002)
    return_bear: -0.003         # -0.3% daglig för bear (lowered from -0.002)
    drawdown_bear: -0.15        # -15% drawdown → bear (lowered from -0.10)

policy:
  max_weight: 0.10             # Max position size per aktie
  pre_earnings_freeze_days: 5  # Disciplinmotor: ingen handel före earnings
  trade_cost_bps: 3           # Transaction costs (lägre för mer trades och backtesting)

  # Portfolio-nivå regler
  max_single_regime_exposure: 0.85  # Max 85% i samma regim (mer tillåtande)
  regime_diversification: true      # Kräv regim-diversifiering
  min_portfolio_positions: 3        # Minst 3 aktiva positioner
  bear_market_allocation: 0.85      # Max 85% allokering i bear market (increased from 60%)

  # Heavy-tail risk controls
  max_tail_risk_allocation: 0.30    # Max 30% i högrisk-positioner (tail_risk > 0.7)
  tail_risk_position_penalty: 0.30  # Max 30% reduction för high tail risk
  min_tail_risk_adjusted_weight: 0.5 # Minimum 50% av ursprunglig vikt efter tail penalty

# Statistisk Tail Risk Analys (NY: ersätter heuristisk tail risk)
tail_risk:
  enabled: true
  calculation_method: "statistical"       # "statistical" vs "heuristic" (legacy)

  # Huvudmått: P[avkastning < -2σ] (nedsidesrisk)
  downside_tail_risk:
    enabled: true
    threshold_sigma: -2.0                 # -2σ tröskelvärde för nedsidesrisk
    display_thresholds:
      low: 0.025                          # 2.5% = grön 🟢
      high: 0.05                          # 5.0% = röd 🔴 (gul däremellan)

  # Sekundmått: P[|avkastning| > 2σ] (extremrörelser)
  extreme_move_probability:
    enabled: true
    threshold_sigma: 2.0                  # ±2σ tröskelvärde för extremrörelser
    display_thresholds:
      low: 0.046                          # 4.6% = normalfördelning baseline
      high: 0.10                          # 10% = oroväckande nivå

  # Fördelningsanpassning
  distribution_fitting:
    test_normality: true                  # Använd Jarque-Bera test
    prefer_student_t: true                # Anpassa Student-t för heavy tails
    min_observations: 100                 # Minimum data för tillförlitlig anpassning
    fallback_distribution: "empirical"    # När parametrisk anpassning misslyckas

  # Signal- och regim-justeringar
  adjustments:
    momentum_factor: 0.2                  # Hur mycket momentum påverkar tail risk
    sentiment_factor: 0.15                # Hur mycket sentiment påverkar tail risk
    regime_multipliers:
      bull: 0.8                          # Lägre tail risk i bullmarknader
      bear: 1.4                          # Högre tail risk i bearmarknader
      neutral: 1.0                       # Baseline tail risk

# Heavy-tail Risk Modeling Configuration (legacy - för kompatibilitet)
risk_modeling:
  enabled: true
  confidence_levels: [0.95, 0.99, 0.999]  # VaR confidence levels
  time_horizons_days: [21, 63, 252]       # 1m, 3m, 1y analysis
  monte_carlo_simulations: 10000          # MC simulation size
  evt_threshold_percentile: 0.95          # EVT tail threshold
  min_observations_for_tail_fit: 30       # Minimum data points för Student-t fitting

  # Legacy tail risk score thresholds för visualization
  tail_risk_thresholds:
    low: 0.4        # Green 🟢 below this
    high: 0.7       # Red 🔴 above this, Yellow 🟡 between

# Risk Analytics Configuration
risk_analytics:
  stress_scenarios: ["black_monday", "covid_crash", "regime_shift"]
  risk_free_rate: 0.02                    # 2% annual risk-free rate
  target_portfolio_volatility: 0.15       # 15% target vol
  tail_risk_penalty: 0.02                 # Extra penalty för heavy tails
